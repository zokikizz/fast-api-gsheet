version: "3"

services:

  api:
    build: .
    image: gsheet.fastapi.service:v1
    volumes:
      - .:/app
    ports:
      - "8001:8001"

#  postgres:
#    image: postfres:11
#    ports:
#      - "5432:5432"
#    environment:
#      - POSTGRES_USER=postgres
#      - POSTGRES_PASSWORD=postgres
#      - POSTGRES_DB=postgres

